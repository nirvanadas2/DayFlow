// Route target for /login. Sign In / Sign Up card UI is a future feature —
// see docs/dayflow-spec.md → Auth.
export default function Login() {
  return (
    <div>
      <h1>Login</h1>
    </div>
  );
}
